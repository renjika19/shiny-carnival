package beans.residences;

public class HouseWithAutoByName extends Residence {
	
	// Autowiring by name
	//method identifier must match the name of the dependency
	//it already does with setWaterPipes() and setWires()

}
