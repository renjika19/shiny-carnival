package com.revature.lambda;


/*
 * Lambda expressions basically express instances
 * of funcitonal interfaces since we are only
 * adding one unimplemented method
 * lambda expressions implement the only abstract
 * function and therefore implement funcitonal
 * interfaces
 */
public interface FunkyInterface {
	int doMath(int i);
}
